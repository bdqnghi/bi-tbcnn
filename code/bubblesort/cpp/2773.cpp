#include<iostream>
#include<vector>
#include<string>

// impliment this as a function alse

// take value a[i]
// check if a[i] > a[i+1] 
//     yes then swap a[i] with a[i+1]
//     no then repeat for i= i+1


int main()
{ 
    int size;
    std::cout<<"enter the size of the array"<<std::endl;
    std::cin>>size;
    const int max =size;

    std::vector <int> seq(max);

    // pushing values in the vector 

}

 
